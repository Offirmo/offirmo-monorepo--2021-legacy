import { CheckedNode } from '../types';
declare function is_list($node: CheckedNode): boolean;
declare function is_link($node: CheckedNode): boolean;
declare function is_KVP_list($node: CheckedNode): boolean;
export { is_list, is_link, is_KVP_list, };
