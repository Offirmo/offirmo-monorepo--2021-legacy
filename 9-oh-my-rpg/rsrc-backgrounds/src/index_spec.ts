import { expect } from 'chai'

import { LIB } from './consts'

import { ELEMENTS } from '.'

describe(`${LIB} - rsrcs`, function() {

	ELEMENTS.forEach(artwork => {

		describe('artworks', function() {

		})
	})
})
