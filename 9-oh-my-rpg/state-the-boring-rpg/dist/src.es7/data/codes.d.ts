import { CodeSpec } from '@oh-my-rpg/state-codes';
import { State } from '../types';
declare const CODE_SPECS_BY_KEY: {
    [key: string]: Readonly<CodeSpec<State>>;
};
export { CODE_SPECS_BY_KEY, };
