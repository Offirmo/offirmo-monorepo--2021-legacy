/*
function get(raw_word) {

	const words = db.findWords(raw_word)

	return words.map(word => {
		// stress
		// pronunciation
		// phonetics
		// links to videos, pages
		// links to pronunciation
		// collocations
		// etc.
	})
}
*/
