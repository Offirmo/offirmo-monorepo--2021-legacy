"use strict";

const BASE_ROUTE = PUBLIC_PATH

const ROUTES = {
	index: '/index.html', // technical route for redirection

	home: '/',
	inventory: '/inventory',
	character: '/character',
	about: '/about',

	x: '/x',
}

export {
	BASE_ROUTE,
	ROUTES,
}
