import { getLogger } from '@offirmo/universal-debug-api-placeholder'

/////////////////////////////////////////////////

const logger = getLogger({
	name: 'cloud-offline-first',
	//suggestedLevel: CHANNEL === 'dev' ? 'silly' : 'warning',
})

export default logger
